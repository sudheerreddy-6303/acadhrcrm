# Used automatically when the frontend is built for production (vite build),
# which is what Railway runs. Points at your deployed backend.
VITE_API_URL=https://acadhrcrm-backend-production.up.railway.app/api
